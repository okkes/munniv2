// GENERATED default — scripts/bake-catalog.mjs overwrites this at CI
// build time with the then-current published catalog, so fresh installs
// (and offline profiles, forever) carry the latest baseline. null means
// "no publish baked in": the hardcoded bundled arrays apply as-is.
import type { CatalogDoc } from '@/domain/catalogDoc';

export const CATALOG_BASELINE: CatalogDoc | null = null;
